# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# VITE_SPEECHLY_APP_ID: Optional. For speech-to-text on iOS/Safari. Get free at https://api.speechly.com/dashboard/
# Without this, voice input works on Chrome/Edge/Android only.
# VITE_SPEECHLY_APP_ID="your-speechly-app-id"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# VITE_IMGBB_API_KEY: Optional. For sharing images as URLs (works in iOS PWA).
# Get a free key at https://api.imgbb.com/ — enables share sheet with image URL instead of download.
# VITE_IMGBB_API_KEY="your-imgbb-api-key"
